# LapN — catch-all default server: block access via IP / unknown Host.
# Installed once by install.sh into sites-available/ + symlinked into sites-enabled/.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
