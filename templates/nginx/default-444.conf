# LapN — catch-all default server: chặn truy cập bằng IP / Host lạ.
# Cài một lần bởi install.sh vào sites-available/ + symlink sang sites-enabled/.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
