# LapN — rate limit zones. Placed in http{} via conf.d/ (installed once by install.sh).
limit_req_zone $binary_remote_addr zone=lapn_general:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=lapn_auth:10m    rate=2r/s;
